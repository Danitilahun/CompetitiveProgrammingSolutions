def superDigit(n, k):
    # Write your code here
    def fun(n):
        if n%10==n:
            return n
        summ=0
        temp=0
        while n:
            summ+= n%10
            n//=10
        n=summ
        return fun(n)
    return fun(int(n*k))
